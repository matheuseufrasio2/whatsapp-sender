import { AppContainer } from './App.styled'
import { AppRoutes } from './routes'

function App(): JSX.Element {
  return (
    <AppContainer>
      <AppRoutes />
    </AppContainer>
  )
}

export default App
